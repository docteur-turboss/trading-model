export const HEARTBEAT = "heartbeat" as const;
export const HEARTBEAT_ACK = "heartbeat.ack" as const;

export const REGISTER = "register" as const;
export const SUBSCRIBE = "subscribe" as const;
export const DISCONNECT = "disconnect" as const;
export const DRAIN = "drain" as const;
export const JOB_ASSIGNED = "job.assigned" as const;
