apiVersion: v1
kind: Service
metadata:
  name: mongo-dlq-headless
  labels:
    app.kubernetes.io/component: mongodb
    app.kubernetes.io/part-of: trading-model
spec:
  clusterIP: None
  ports:
    - port: 27017
      targetPort: 27017
      name: mongodb
  selector:
    app.kubernetes.io/component: mongodb
---
apiVersion: v1
kind: Service
metadata:
  name: mongo-dlq
  labels:
    app.kubernetes.io/component: mongodb
    app.kubernetes.io/part-of: trading-model
spec:
  ports:
    - port: 27017
      targetPort: 27017
      name: mongodb
  selector:
    app.kubernetes.io/component: mongodb
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongo-dlq
  labels:
    app.kubernetes.io/component: mongodb
    app.kubernetes.io/part-of: trading-model
spec:
  serviceName: mongo-dlq-headless
  replicas: 3
  selector:
    matchLabels:
      app.kubernetes.io/component: mongodb
  template:
    metadata:
      labels:
        app.kubernetes.io/component: mongodb
        app.kubernetes.io/part-of: trading-model
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/component: mongodb
                topologyKey: kubernetes.io/hostname
      terminationGracePeriodSeconds: 60
      containers:
        - name: mongodb
          image: mongo:7
          command:
            - mongod
          args:
            - "--replSet"
            - "rs0"
            - "--port"
            - "27017"
            - "--bind_ip"
            - "0.0.0.0"
            - "--enableEncryption"
            - "--encryptionKeyFile"
            - "/etc/mongo-encryption/key"
          ports:
            - containerPort: 27017
              name: mongodb
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 2Gi
          volumeMounts:
            - name: mongo-dlq-data
              mountPath: /data/db
            - name: mongo-encryption-key
              mountPath: /etc/mongo-encryption
              readOnly: true
          livenessProbe:
            exec:
              command:
                - mongosh
                - --quiet
                - --eval
                - "try { rs.status().ok } catch { 0 }"
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
          readinessProbe:
            exec:
              command:
                - mongosh
                - --quiet
                - --eval
                - "db.adminCommand('ping').ok"
            initialDelaySeconds: 15
            periodSeconds: 5
            timeoutSeconds: 3
      volumes:
        - name: mongo-encryption-key
          secret:
            secretName: mongodb-encryption-key
            optional: false
            defaultMode: 0400
  volumeClaimTemplates:
    - metadata:
        name: mongo-dlq-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 20Gi
---
apiVersion: batch/v1
kind: Job
metadata:
  name: mongo-dlq-init
  labels:
    app.kubernetes.io/component: mongodb
    app.kubernetes.io/part-of: trading-model
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
spec:
  template:
    spec:
      restartPolicy: OnFailure
      containers:
        - name: mongodb-init
          image: mongo:7
          command:
            - mongosh
            - --host
            - mongo-dlq-0.mongo-dlq-headless:27017
            - --eval
            - |
              rs.initiate({
                _id: "rs0",
                members: [
                  { _id: 0, host: "mongo-dlq-0.mongo-dlq-headless:27017" },
                  { _id: 1, host: "mongo-dlq-1.mongo-dlq-headless:27017" },
                  { _id: 2, host: "mongo-dlq-2.mongo-dlq-headless:27017" }
                ]
              })
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: mongo-dlq-pdb
  labels:
    app.kubernetes.io/component: mongodb
    app.kubernetes.io/part-of: trading-model
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app.kubernetes.io/component: mongodb
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: mongodb-network-policy
  labels:
    app.kubernetes.io/component: mongodb
    app.kubernetes.io/part-of: trading-model
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/component: mongodb
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: trading-model
      ports:
        - port: 27017
          protocol: TCP
