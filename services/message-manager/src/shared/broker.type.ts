/**
 * Re-exports broker type definitions from `@trading-model/common/config/tls-paths`.
 * Prefer importing from there directly in new code.
 */

export type { TlsPaths as BrokerConfig } from "@trading-model/common/config/tls-paths";
