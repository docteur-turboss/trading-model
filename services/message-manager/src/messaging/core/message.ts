/**
 * Re-exports message contract types from `@trading-model/validation/contracts/message.types`.
 * Prefer importing from there directly in new code.
 */

export type {
	Message,
	MessageMetadata,
} from "@trading-model/validation/contracts/message.types";
